<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.arunm.cseelection2017">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_launcherround"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SubmitVoteMain">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".WinnersActivity" />
        <activity android:name=".TeacherVotesLegend"></activity>
    </application>

</manifest>